#!/usr/bin/env bash
set -euo pipefail

echo "=== pre-commit: checking Rust formatting ==="
cargo fmt --all -- --check

echo "=== pre-commit: running clippy ==="
cargo clippy -p matplotlib-python --no-deps -- -D warnings

echo "pre-commit passed"
